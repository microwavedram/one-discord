(() => {const crypto = require('crypto');require('dotenv').config();module.exports = {key: (() => {const decipher = crypto.createDecipheriv('aes-256-cbc', crypto.scryptSync("https://www.youtube.com/watch?v=dQw4w9WgXcQ", "https://printer.discord.com/", 32), Buffer.alloc(16, 0));let decrypted = decipher.update(process.env.TOKEN, 'hex', 'utf8');decrypted += decipher.final('utf8');return decrypted})}})()